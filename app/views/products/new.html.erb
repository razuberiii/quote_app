<section class="workspace-page workspace-page--editor">
  <header class="workspace-heading">
    <span class="eyebrow">Library / Products / New</span>
    <h1>Product source</h1>
    <p>Create a reliable product record while staying close to the quote workflow.</p>
  </header>

  <%= render "form",
             product: @product,
             submit_label: t("products.view.new.submit_label"),
             back_path: products_path,
             back_label: t("products.view.new.back_to_products") %>
</section>
