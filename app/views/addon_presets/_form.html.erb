<%= form_with model: addon_preset, class: "settings-form-grid" do |f| %>
  <div>
    <%= f.label :name, t("addon_presets.view.form.preset_name") %>
    <%= f.text_field :name, placeholder: t("addon_presets.view.form.name_placeholder"), class: ui_input_classes %>
  </div>

  <div class="settings-grid-span-2">
    <%= f.label :entries_text, t("addon_presets.view.form.addons_text") %>
    <%= f.text_area :entries_text, value: addon_preset.entries_text, rows: 8, placeholder: t("addon_presets.view.form.entries_placeholder"), class: ui_input_classes %>
    <p class="team-members-hint"><%= t("addon_presets.view.form.hint_html") %></p>
  </div>

  <div class="settings-grid-span-2 settings-actions">
    <%= f.submit(addon_preset.persisted? ? t("addon_presets.view.form.update") : t("addon_presets.view.form.create"), class: "btn btn-primary") %>
  </div>
<% end %>
