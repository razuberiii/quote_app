<section class="app-page-shell">
  <header class="app-page-header">
    <div class="app-page-header-main">
      <% if @quote.quote_no.present? %>
        <h1><%= t("quotes.view.new.revision_title") %></h1>
        <p><%= t("quotes.view.new.revision_subtitle", quote_no: @quote.quote_no) %></p>
      <% else %>
        <h1><%= t("quotes.view.new.quote_title") %></h1>
        <p><%= t("quotes.view.new.quote_subtitle") %></p>
      <% end %>
    </div>
    <div class="app-page-header-actions">
      <%= quote_status_badge(@quote.status.presence || "draft") %>
    </div>
  </header>

  <% revision_mode = @source_quote.present? %>
  <%= render "form",
             quote: @quote,
             form_model: [ @customer, @quote ],
             submit_label: (revision_mode ? t("quotes.view.new.create_revision") : nil) %>

  <% if revision_mode %>
    <%= link_to t("quotes.view.new.back_to_quote"), quote_path(@source_quote), class: "form-page-back-link" %>
  <% else %>
    <%= link_to t("quotes.view.show.back_to_customer"), customer_path(@customer), class: "form-page-back-link" %>
  <% end %>
</section>
